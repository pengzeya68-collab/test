# 2026-04-11 工作日志

## Phase E: 路由对齐与清理（全部完成）

### 修复的问题
1. **BASE_URL 端口错误**: `autotest_settings.py` 默认端口从 5002 改为 5001
2. **后端缺失5个路由**:
   - `POST /api/auto-test/send` — 接口调试发送（替代旧的 /interface-test/send 和 /auto-test/send）
   - `GET /api/auto-test/tasks/{task_id}` — 任务状态轮询
   - `POST /api/auto-test/tasks/{task_id}/cancel` — 取消任务
   - `POST /api/auto-test/scheduler/tasks/{task_id}/toggle` — 切换定时任务启用/暂停
   - `GET /api/auto-test/reports/{report_id}` — 获取报告详情
3. **前端路由修复** (3个文件):
   - AutoTest.vue: `/auto-test/send` → `/api/auto-test/send`, `/api/interface-test/environments` → `/api/auto-test/environments`, `/api/interface-test/send` → `/api/auto-test/send`
   - ScenarioList.vue: `/api/interface-test/environments` → `/api/auto-test/environments`, `/api/auto-test/scenarios/history/{id}` → `/api/auto-test/history/{id}`
   - ScenarioEditor.vue: `/api/interface-test/environments` → `/api/auto-test/environments`
4. **根目录清理**: 53个临时.py脚本已删除（调试/迁移/测试用）
5. **scripts/ 路径修复**: `restore_auto_test_data.py` 和 `create_default_env.py` 中 `auto_test_platform/auto_test.db` → `instance/auto_test.db`

### 新增架构
- 场景执行支持 `_task_store` 内存任务状态存储（替代 Celery）
- 调度器新增 `toggle_task_status()` 函数支持启用/暂停切换

### 验证结果
- FastAPI app 成功加载，182 个路由全部注册正确

## Phase F: 前后端路由全面对齐（追加修复）

### 新发现并修复的问题
6. **InterfaceTest 模块前端 API 路径修复**（4个文件20+处替换）:
   - InterfaceTestList.vue: `/interface-test/folders` → `/api/groups/`, `/interface-test/cases` → `/api/cases/`, `/interface-test/plans` → `/api/plans/`, `/interface-test/environments` → `/api/environments/`, `/interface-test/reports` → `/api/reports`, `/interface-test/send` → `/api/auto-test/send`, `/interface-test/cases/{id}/history` → `/api/cases/{id}/history`
   - InterfaceTestDetail.vue: 同类路径修复
   - InterfaceEnvManager.vue: `/interface-test/environments` → `/api/environments/`
   - EnvironmentManager.vue: 同上
7. **后端补路由**: `/api/cases/{case_id}/history` — 用例执行历史（跨库查询 auto_test.db）
8. **面试模块路由修复**（2个文件5处）:
   - InterviewSimulate.vue: `/interview/categories` → `/api/v1/interview/questions`, `/interview/sessions/create` → `/api/v1/interview/sessions`, `/interview/sessions/{id}` → `/api/v1/interview/sessions/{id}`, `/interview/sessions/{id}/submit-answer` → `/api/v1/interview/sessions/{id}/submissions`, `/interview/sessions/{id}/complete` → `/api/v1/interview/sessions/{id}/submissions`
   - InterviewDetail.vue: `/interview/sessions/{id}` → `/api/v1/interview/sessions/{id}`
9. **登录路由修复**（3个文件）:
   - Login.vue, admin/Login.vue, admin/views/Login/index.vue: `/login` → `/api/v1/auth/login`

### 尚未实现的后端路由（admin 管理后台）
以下前端调用的 `/admin/` 路由后端暂未实现，属于未来工作：
- `/admin/login`, `/admin/info`, `/admin/dashboard/stats`
- `/admin/users/*` (CRUD + toggle)
- `/admin/exercises/*`, `/admin/exams/*`, `/admin/paths/*`
- `/admin/audit-logs`, `/admin/system/metrics`
- `/register`, `/profile`, `/change-password`
