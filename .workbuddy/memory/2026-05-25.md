# 2026-05-25 工作日志

## 修复 - 代码质量收口 (P0-P2)

### P0: AutoTest 请求前缀修复
- 问题: autoTestRequest baseURL 已是 `/api`，但调用时仍写 `/api/auto-test/...`，变成 `/api/api/auto-test/...`
- 修复: 使用 sed 批量替换 9 个文件、73 处引用，`/api/auto-test/` → `/auto-test/`
- 文件: EnvironmentManager.vue, GlobalVariableManager.vue, ScenarioExecutionDialog.vue, Settings.vue, AutoTest.vue, CaseEditorDrawer.vue, CaseList.vue, ScenarioEditor.vue, ScenarioList.vue

### P0: 项目实战统一访问控制
- 问题: 所有项目接口只校验"项目存在"，不校验用户是否有权访问
- 修复: 添加 `_can_access_project()` 和 `_require_project_access()` 函数
  - Admin 用户: 可访问所有项目
  - 普通用户: 只能访问 is_public=True 的学习路径下、已发布的项目
- 全部 9 个端点已添加权限校验: 详情/任务/资源/开始/进度/提交/评价/autotest/考试
- 文件: fastapi_backend/routers/projects.py

### P0: 项目-场景归属校验
- 问题: project_autotest_run 只验证 scenario_id 存在，不验证场景是否属于该项目
- 修复: 
  1. 添加 `project_id` 字段到 AutoTestScenario 模型 (nullable, 向后兼容)
  2. 添加自动列迁移逻辑 (autotest_database.py)
  3. 在 project_autotest_run 中校验 scenario.project_id
- 文件: models/autotest.py, core/autotest_database.py, routers/projects.py

### P1: 数据模板更新校验对齐
- 问题: TestDataTemplateUpdate 缺少 name/scenario_id/row_count/fields 约束
- 修复: 给 Update schema 添加与 Create 一致的字段约束 (min_length, max_length, ge, le, gt, field_validator)
- 文件: schemas/autotest.py

### P1: ScenarioEditor 异常处理
- 问题: `axios.isCancel(error)` 但 axios 未导入，请求异常时可能再抛错
- 修复: 添加 `import axios from 'axios'`
- 文件: frontend/src/views/ScenarioEditor.vue

### P1: 数据驱动表格单元格自动保存
- 问题: 表头改动有 @change 自动保存，但表格单元格只做 v-model 无保存钩子
- 修复: 给单元格 `<el-input>` 添加 `@change="handleDatasetChange"` (1秒防抖自动保存)
- 文件: frontend/src/views/ScenarioEditor.vue

### P2: 数据工厂高级规则编辑态
- 问题: uuid/timestamp/phone/email/username 规则后端支持但前端编辑器缺少配置输入
- 修复:
  1. 添加 5 个新的 `<el-input>` 编辑组件 (带 v-else-if)
  2. 添加 updateUuidConfig/updateTimestampConfig/updatePhoneConfig/updateEmailConfig/updateUsernameConfig 处理函数
  3. 更新 onRuleTypeChange 初始化 configInput
  4. 更新 buildConfigInput 回填格式
  5. 更新 formatConfig 显示格式
- 文件: frontend/src/views/DataFactory.vue

## 新增 - 专业测试学习站功能补齐

### 通知系统
- 后端: Notification 模型 (models/models.py) + 路由 (routers/notifications.py, 5个端点)
- 前端: NotificationBell.vue 组件 (铃铛+未读数+下拉面板+30秒轮询), Notifications.vue 列表页
- 集成: App.vue 导航栏铃铛 + 用户菜单入口 + 路由注册
- 支持类型: reply/exam_start/exam_deadline/project_feedback/achievement/system/learning_reminder

### 收藏夹功能
- 扩展 Favorite 模型: 新增 exercise_id/note_id/item_type 字段 (多态收藏)
- 后端: routers/favorites.py (GET列表/POST切换/GET检查, 3个端点)
- 前端: Favorites.vue 页面 (分类tab/取消收藏/点击跳转)
- 支持收藏: 帖子(post)/习题(exercise)/笔记(note)

### 全局搜索
- 后端: routers/search.py (跨模块搜索: 习题/帖子/考试/学习路径)
- 前端: SearchResults.vue 页面 (分类TAB切换) + Home.vue 搜索栏
- 路由: /search

### 测试工具导航
- 后端: routers/tools.py (6大分类25个工具, 内置数据)
- 前端: TestingTools.vue 页面 (卡片网格布局)
- 公用导航栏链接 + 用户菜单入口

## 新增 - 专业测试核心功能

### 断言模板库
- 后端: routers/assert_templates.py (15个预置模板, 6大分类)
- 前端: CaseEditorDrawer.vue 添加"模板"按钮 + 选择对话框 + 一键注入
- 模板: 状态码/响应结构/性能/数据结构/业务逻辑/综合模板

### API Diff 对比工具
- 后端: routers/autotest_diff.py (深度JSON diff 算法)
- 支持: 字段新增/删除/类型变更/值差异全量对比
- 返回: diffs列表 + summary统计(added/removed/changed)

### 回归测试套件
- 后端: routers/autotest_suites.py (CRUD + execute + runs history)
- 支持: 套件管理/批量执行/执行历史

### 环境健康检查
- 后端: routers/autotest_health.py (全量+单环境检查)
- 支持: aiohttp异步健康检查, 超时/错误处理, 摘要统计

## 修复 - 死代码清理 & 测试体系收口

### 删除的死代码
- 5个legacy路由 (cases/groups/environments/plans/execution.py, 877行)
- main.py 中 legacy_test_router 注册
- 41个根目录临时脚本
- 20+个输出文件 (.txt/.csv/.out)
- 2个孤儿db文件 + celery遗留 + 旧日志
- auto_test_platform残余目录

### insert_path 脚本修复
- 所有18个 insert_path*_*.py 脚本中中文引号冲突已修复
- insert_path2_exercises.py: 2处语法错误 (行31-32, 63-64)
- insert_path8_exercises.py: 23处语法错误 (行72, 97等)
- 修复策略: 外层双引号→单引号，或使用 Unicode 转义

### 测试体系优化
- conftest.py 重构: session 级 create_all + 函数级 truncate
- 减少了每条测试重建所有 schema 的开销
- 从 function-scope 全量 create/drop → session-scope 建表 + truncate
- 测试结果: 79 passed, 5 skipped, 1 failed (跨DB测试)

### 前端优化
- Sass @import → @use 尝试后因兼容性回退(@import仅告警)
- vite.config.js 添加 manualChunks 分拆 element-plus/echarts/xlsx/vue
- 构建成功, 分块生效

## 最终收口 - 测试体系 + 全量编译 (第3轮)

### A1-A2: regenerate/insert/seed/generate 脚本修复
- regenerate_lp12_18.py: 276行中文引号冲突
- 38个目标脚本全部修复: regenerate_lp12_18 + 18个insert_path + seed_*/generate_*
- 策略: value 内 `"` → `'`

### C1/C2: test_interview_flow.py
- 从 `@patch('get_current_active_user')` → `app.dependency_overrides[...]`
- 断言收紧到明确范围
- 0.41s 全部通过

### D1: test_sandbox.py
- AsyncMock → MagicMock(side_effect=TimeoutError)
- 消除未 await 协程警告

### E1: schemas/rbac.py
- `class Config: orm_mode = True` → `model_config = ConfigDict(from_attributes=True)`
- Pydantic 弃用警告: 5→1

### 最终结果
- ✅ 38个目标脚本全部编译通过
- ✅ 78 passed, 5 skipped, 1 failed
- ✅ 前端构建通过

### 验证结果
- ✅ 18个insert_path脚本编译通过
- ✅ 79个测试通过 (仅1个跨DB测试失败)
- ✅ 前端构建成功

### 学习报告导出
- 后端: routers/reports.py (summary摘要 + export文本导出)
- API: /api/v1/report/summary, /api/v1/report/export

### API Mock 服务
- 后端: routers/mock_api.py (创建规则/列出规则/删除规则/动态Mock端点)
- 支持数据类型: string/number/boolean/array/object/uuid/timestamp/email/phone
- 支持延迟模拟(delay_ms)

### 首页Dashboard增强
- Home.vue: 新增搜索栏 + 收藏/项目/工具导航入口 + 更丰富的统计卡片

### 路由注册
- main.py 新增6个 router 注册
- frontend/src/router/index.js 新增5个路由: /search, /tools, /favorites, /notifications, /search
- App.vue 用户菜单扩展: 收藏/通知/工具导航
