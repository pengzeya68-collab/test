# 2026-04-12 工作日志

## Admin 后端路由补全 & 前端路由对齐（最终完成）

### 完成事项
1. **admin_manage.py 修正**: 将 `from fastapi_backend.core.security import get_password_hash` (不存在的模块) 改为 `AuthService.hash_password()`，共3处
2. **admin_manage.py 补全路由**: 添加以下缺失的路由
   - `POST /login` — 管理员登录
   - `GET /info` — 获取管理员信息
   - `PATCH /users/{id}/status` — 兼容 api/admin.js 的 PATCH 调用
   - `GET /learning-paths` — 兼容 /admin/learning-paths 路径
   - `GET /learning-paths/{id}` / `POST` / `PUT` / `DELETE` — 完整兼容路径
   - `GET /exercises/{id}` — 获取习题详情
   - `POST /exercises/batch-import` — 批量导入习题
   - `GET /backups` / `POST` / `DELETE /old` / `GET /download/{name}` / `POST /{name}/restore` / `DELETE /{name}` — 完整备份管理
   - `GET /audit-logs` — 审计日志
   - `GET /system/metrics` — 系统指标
3. **前端 Admin Vue 组件修复**（上次会话完成）
   - Users.vue — 替换 Mock 数据为真实 API 调用
   - Exercises.vue — 替换 Mock 数据为真实 API 调用
   - LearningPaths.vue — 替换 Mock 数据为真实 API 调用
   - Dashboard.vue — 修复 axios.get → request.get
   - Settings.vue — 已使用统一 request 客户端
4. **端到端验证**: 47+ 个前端 API 调用与后端路由全部对齐

### 架构说明
- Admin 路由分两个文件共享 prefix `/api/v1/admin`:
  - `admin.py` — 面试题管理 (CRUD + 测试用例 + 统计)
  - `admin_manage.py` — 用户/习题/学习路径/考试/社区/仪表盘/备份/审计/系统管理
- 两个 router 都在 main.py 中注册
- 密码操作统一使用 `AuthService.hash_password()` 和 `AuthService.verify_password()`
