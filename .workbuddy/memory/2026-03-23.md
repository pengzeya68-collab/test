# 2026-03-23 工作记录

## 火山方舟（豆包大模型）接入测试

- **任务**: 启动服务并测试火山方舟 API
- **配置**:
  - API Key: 32239c0c-10b3-44bd-bc89-82f248026abf
  - 模型: doubao-pro-32k
  - 基础URL: https://ark.cn-beijing.volces.com/api/v3

## 问题解决

1. **数据库路径问题**: Flask 自动重启时工作目录改变导致数据库文件无法访问
   - 解决方案: 在 backend/app.py 开头设置工作目录为项目根目录
   - 禁用 debug 模式避免自动重启
   - 修复数据库路径：使用绝对路径并转换为正斜杠

2. **启动方式**:
   - 后端: 使用 start-backend-ark-background.ps1 在新窗口启动
   - 前端: 使用 npm run dev 启动

3. **端口调整**:
   - 后端: 从5000改为3000
   - 前端: 保持5173，API代理从5000改为3000

## 接口测试功能修复

1. **环境管理页面模板语法错误**
   - **文件**: `frontend/src/views/InterfaceEnvManager.vue`
   - **错误**: 第41行 `{{ env.base_url || '(空)' }}` 多了一个右花括号
   - **修复**: 将 `}}` 改为 `}`

2. **FolderPlus 图标不存在**
   - **文件**: `frontend/src/views/InterfaceTestList.vue`
   - **错误**: Element Plus 没有 `FolderPlus` 图标
   - **修复**: 替换为 `FolderAdd` 图标

3. **数据库文件无法打开**
   - **错误**: `sqlalchemy.exc.OperationalError: unable to open database file`
   - **原因**: 数据库路径使用反斜杠导致问题
   - **修复**: 使用绝对路径并转换为正斜杠格式

## 文件整理

- **清理**: 将 14 个测试脚本从桌面移动到项目的 `scripts/` 目录
- **避免**: 不再在桌面创建临时文件，所有文件都放在项目目录内

## 服务状态

- 后端: http://localhost:3000 (已启动，PID: 1408)
- 前端: http://localhost:5173 (已启动)

## 服务状态

- 后端: http://localhost:3000 (已启动，PID: 4484)
- 前端: http://localhost:5173 (已启动，PID: 22196)
