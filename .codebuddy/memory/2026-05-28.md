# 2026-05-28 工作日志

## BUG修复：AutoTest引导步骤跳转JMeter
- **问题**: 点击引导第5步"JMeter压测"后，`activeTab='jmeter'` 但 `visitedTabs` 没有 `'jmeter'`，`v-if` 不渲染
- **修复**: `guideTips[].action` 统一改为 `() => handleTabChange('xxx')` 而非直接改 activeTab
- **文件**: `frontend/src/views/AutoTest.vue`

## BUG修复：CaseEditorDrawer 全屏自适应(3次迭代)
- **第1版**: `computed()` 基于 `window.innerWidth` → 不响应全屏事件
- **第2版**: `ref` + `resize`/`fullscreenchange` 事件 → Element Plus 内部缓存导致不生效
- **第3版(最终)**: `ref` + 事件 + `watch` 强制 `$el.querySelector('.el-drawer.rtl').style.setProperty('width', size, 'important')` 直接操作DOM
- **文件**: `frontend/src/views/CaseEditorDrawer.vue` (改动: `drawerSize` ref + watch DOM操作, `mainDrawerRef`)

## 部署流程(重要)
- **Dockerfile 不构建前端**！nginx 用 bind mount `./frontend/dist:/usr/share/nginx/html`
- **每次部署必须**: 本地 `npm run build` → SCP dist 到服务器 → `docker compose up -d --build`
- 源码 git push + 服务器 git pull 更新后端，dist 需要额外上传
