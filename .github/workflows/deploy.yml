name: CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  lint-backend:
    name: Lint Backend
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install ruff
          pip install -r requirements.txt

      - name: Ruff check
        run: ruff check fastapi_backend/ --output-format=github

      - name: Ruff format check
        run: ruff format fastapi_backend/ --check

  lint-frontend:
    name: Lint Frontend
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: ESLint
        run: npx eslint src/ --max-warnings 0

      - name: Build check
        run: npm run build

  test-backend:
    name: Backend and PostgreSQL Integration Tests
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_DB: testmaster_ci
          POSTGRES_USER: testmaster
          POSTGRES_PASSWORD: testmaster_ci_password
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U testmaster -d testmaster_ci"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      DATABASE_URL: postgresql+asyncpg://testmaster:testmaster_ci_password@localhost:5432/testmaster_ci
      TEST_POSTGRES_URL: postgresql+asyncpg://testmaster:testmaster_ci_password@localhost:5432/testmaster_ci
      ENVIRONMENT: testing
      SECRET_KEY: ci-test-secret-key
      ADMIN_PASSWORD: ci-test-admin-password
      ADMIN_SECRET_KEY: ci-test-admin-secret-key
      AUTO_CREATE_TABLES_ON_STARTUP: "false"
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Apply PostgreSQL migrations
        run: python -m alembic -c fastapi_backend/alembic.ini upgrade head

      - name: Run PostgreSQL schedule lease test
        run: python -m pytest fastapi_backend/tests/test_postgres_schedule_lease.py -q

      - name: Run isolated backend test suite
        env:
          DATABASE_URL: "sqlite+aiosqlite:///:memory:"
          TEST_POSTGRES_URL: ""
        run: python -m pytest fastapi_backend/tests -q

  security-scan:
    name: Security Scan
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Check for hardcoded secrets
        continue-on-error: true
        run: |
          echo ">>> Scanning for potential hardcoded secrets..."
          # Check for common patterns (excluding .env.example and this workflow)
          if grep -rn --include="*.py" --include="*.sh" --include="*.bat" \
            -E "(password|passwd|secret|api_key|token)\s*=\s*['\"][^'\"]{8,}['\"]" \
            --exclude-dir=node_modules --exclude-dir=.git --exclude-dir=venv --exclude-dir=.venv \
            --exclude="*.md" --exclude=".env.example" --exclude="deploy.yml" \
            --exclude="seed_lessons*" \
            . 2>/dev/null | grep -v "CHANGE_ME" | grep -v "your-" | grep -v "example" | grep -v "test"; then
            echo "⚠️  Potential hardcoded secrets found. Please review."
            exit 1
          else
            echo "✅ No obvious hardcoded secrets found"
          fi

      - name: Audit Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pip-audit
          pip-audit -r requirements.txt --ignore-vuln PYSEC-2024-* || true

      - name: Audit frontend dependencies
        working-directory: frontend
        run: |
          npm audit --audit-level=high || true

  deploy:
    name: Deploy to Server
    needs: [lint-backend, lint-frontend, test-backend, security-scan]
    if: github.ref == 'refs/heads/main' && github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.2.2
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          script: |
            cd /root/TestMasterProject
            echo ">>> Pulling latest code..."
            git pull origin main
            echo ">>> Rebuilding and restarting Docker services..."
            docker compose up -d --build
            echo ">>> Cleaning old images..."
            docker image prune -f
            echo ">>> Waiting for services to start..."
            sleep 15
            echo ">>> Service status:"
            docker compose ps
            HEALTH=$(curl -s http://localhost:5001/api/health)
            if echo "$HEALTH" | grep -q '"status":"ok"'; then
              echo "✅ Deployment successful - backend is healthy"
            else
              echo "❌ Deployment may have issues - backend health check failed"
              echo "Response: $HEALTH"
              docker compose logs backend --tail 20
              exit 1
            fi
